#ifndef MACROS_H_
#define MACROS_H_

#define VALID 0
#define INVALID 1
#define FUTILE 2

#define SPACE (-1) //for prepare

#define MIN(a,b) ((a) > (b) ? (b) : (a))
#define MAX(a,b) ((a) > (b) ? (a) : (b))

#define SOLVED 1
#define UNSOLVED 0

#endif
